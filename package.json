{
  "name": "creative-anchor-root",
  "version": "1.0.0",
  "description": "Creative Anchor - Desktop Folder Syncing and Burnout Visualizer",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npm install && npm install --prefix local-backend && npm install --prefix frontend",
    "start": "concurrently \"npm start --prefix local-backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
