apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supabase-storage-pvc
  namespace: supabase
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 10Gi
