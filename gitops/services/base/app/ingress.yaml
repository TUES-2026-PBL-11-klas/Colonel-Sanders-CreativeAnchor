apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: creative-anchor
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: creative-anchor
                port:
                  number: 80
